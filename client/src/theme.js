import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#19305a',
        },
        secondary: {
            main: '#e0f4e9',
        },
        background: {
            default: '#f4f4f4',
            paper: '#f9f9f9',
        },
    },
});

export default theme;
